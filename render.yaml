services:
  - type: web
    name: transcript-tool
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: MAX_DURATION_SECONDS
        value: "7200"
      - key: YTDLP_COOKIES_FILE
        sync: false
